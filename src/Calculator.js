const add = (x, y) => (x + y);
const subtract = (x, y) => (x - y);
const divide = (x, y) => (x / y);
const multiply = (x, y) => (x * y);
const square = (x) => (x * x);
const root = (x) => (Math.sqrt(x));

export { add, subtract, divide, multiply, root, square };
