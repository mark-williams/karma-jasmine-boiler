const add = (x, y) => (x + y);
const subtract = (x, y) => (x - y);
const divide = (x, y) => (x / y);
const multiply = (x, y) => (x * y);
const square = (x) => (x * x);

export { add, subtract, divide, multiply, square };
